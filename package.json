{
  "name": "bot.ts",
  "private": true,
  "workspaces": [
    "apps/*",
    "packages/*"
  ],
  "devDependencies": {
    "turbo": "latest"
  },
  "scripts": {
    "build": "turbo build",
    "start": "turbo start",
    "format": "turbo format",
    "test": "turbo test"
  }
}
